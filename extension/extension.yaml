name: custom:dominios-web
version: 0.0.5
minDynatraceVersion: "1.285"
author:
  name: "Dynatrace"

python:
  runtime:
    module: dominios_web
    version:
      min: "3.10"

  activation:
    remote:
      path: activationSchema.json
    local:
      path: activationSchema.json
